- check_format_csv erweitern für rxncon eingabe (das erste elif)
- sollte compund benötigt sein: kann sowohl in weiterer datei oder in
   der selben tabelle vorkommen, also cases abfangen wo zeile mit !
   begint
   - Achtung: kann auch ein weiterem sheet stehen
- es ist auch möglich, dass in einem Dokument mehrere tabellen mit dem
   selben tableType drinne stehen (oder sheet)
- was ist mit tsv format? oder xls? txt?
- delimiters sind irgendwo declared. standart ist tabulator, aber wenn
  nicht steht das irgendwo
- If table cells contain special characters that are also used as cell
  delimiters (e.g. commas), the file must be provided in a form that
  excludes ambiguities (e.g. in the case o a comma-separated table
  containing commas with its fields, all cells must additionally be
  marked by quotation marks ("..")
- users an also define their own table types and corresponding
  columns. This definition must be provided by the user in the form of
  a special Definition table. The default table (containing the
  predefined table an d column types) is available on the SBtab
  website.
- If a compound, enzyme, or genetic regulator is localised in a
  compartment, the corresponding localised entity can be denoted by
  compound [ compartment ] with square brackets, where 6 compound and
  compartment are the shortnames or IDs of the compound and the
  compartment used in the model. If a model contains several
  compartments, tools should treat the first compartment in the
  Compartment table as the standard compartment. The standard
  compartment will be used by default for all compounds that are not
  explicitly assigned to compartments
- reaction formula should represent the actual stoichiometries
  experienced by the en zyme (i.e.  A <=> 2 B) 
  - wichtig, sofern die
    rkt gelichung überhaupt interessant ist (vllt doch mit regexp?)
- Comment lines Table lines starting with a “%” character contain
  comments a nd are ignored during parsin
- Information about enzymes or genes and their regulation can be
  stored in a Reaction table if there is a one-to- one relationship
  between reactions, enzymes, and possibly genes. Otherwise, it is
  stored in separate tables Enzyme and Gene and the tables are
  interlinked via the columns !Enzyme (in table Reaction ) and !Gene (in
  table Enzyme ) or !TargetReaction (in an Enzyme table) and
  !GeneProduct (in a Gene table)
- in der spezi steht, dass es einen parser geben soll, der sbtab
  einliest und python dictionary zurück gibt. finde ich auf der hp
  aber nirgends
- desweiteren steht da, dass die zum lesen die tablib library zum
  parsen verwenden. frag mal timo?