newer:
   - mit profiler alles mal checken:
      - range() wird ziemlich oft gecallt. Vielleicht ein mal abspeichern und dann imemr readen?
   - beim ausführen von test_rxncon_sbtab_parsing() werden einige variablen scheinbar doppelt
     initialisiert. ein mal zB in commandline und dann im dircheck. also das d object hält schon
     einiges und im self sind variablen mit dem selben namen, aber leer.


New:
   Marcus:
      - make sbtab file from rxncon file and make excel color coding depending on:
         grey: not user editable (wie halt in rxncon)
         sth other: neede
         sth other other: optional
         - dazu notizen im Block (Sbtab kapitel) und oder Foto vom Whiteboard angucken (17.09)
      - far future: Script that automatically cecks and updates rxncon files and adds product state and so on with regexp
   Basti:
      - make clean todo list in gitlab using issues
      - finish testing program
      - die ganzen "der inhalt muss in die spalte" stellen einfacher machen:
         - irgendwo eine mapping struktur definieren, die quasi alle zusammenhänge zwischen rxncon und sbtab enthält:
            welche spalte heißt in dem anderen format wie, welcher spaltenindex passt zu dem im anderen format
         - diese dann überall verwenden wo diese ewig langen "spalte hat der wert von blah am index blah" dinger sind
         - vielleicht mit dicts, vllt mit dicts von dicts, vllt andere objekte?
         - for schelife, die indexes_dict generation besser macht? (noch nötig, wenn der vorherige punk tumgesetzte ist?)
       - generell: nach dem wort 'Basti' im code suchen, da sind oft Vorschläge von ihm genannt




*Marcus*:
   - sourcde state und product state werden nicht ausgefüllt, soll ich aber erst mal ignorieren, vllt ändert sich das eh noch
   - hello text verbessern (update sebastian: am ende wird eh nix console sein, sondern alles im Framwork, von daher egal)
   - eigentlich sollten contingencies und reactions automatisch geupdatert werden, wenn man zb die domains ändert. dies geht über links in excel
     wenn aber kein excel verwendet wird sondern ods klappt das nicht so gut. Deswegen wird vllt ein skript benötigt, das das updatet
   - TODO: aus einem rxncon input und sbtab output jeweils ein rxncon object machen und daraus zum beispiel einen bngl txt file exporten. Diese beiden Files dann vergleichen um guete des parsers zu bewerten (testen)
   - außerdem: draft lesen zu syntax and semantics

   *fragen an Marcus:
   - die formel verwendung habe ich jetzt erst mal nur bis coprduct gemacht, weil ab da auch auf andere sheets zugegriffen wird
      - soll das so oder wie gehabt hart rein schreiben

- gucken ob alle benötigten files da sind
   - SBtab --> rxncon: ReactionList, ReactionID, Gene, ContingencyID,
      rxncon_Definition wobei achtung: da gibts 2, eine rxncon seitige und eine sbtab seitige
   - rxncon --> SBtab: Reaction List, Contingency List, Reaction definition   
- gucken ob zu viele files da sind ( mehrere Projekte? Oder kann man auch mehrere reaction files haben? timo fragen. gibts es eine ID für ein Projekt?)

*Sebastian*:
   -reiter (II) löschen, die anderen sollen aber ihre numerierung behalten
   - Markus mail:
      - ORF und non ORF ID's
   - formeln rein in rxncon xls für: state links, links für target in contID aus rection list mit reaction ID als cont ID (pattern matching, dann href)
   - zeile 150, delimiter abfragen, tab als default
   - z 733 folgende: diese info gibt es doch! aufen auf
   - z 766-768 hier das selbe

*rxncon --> sbtab*
- zuerst auch 2 beispiel directories erstellen mit händisch geparstem zeug. Dann kann ich diese einlesen und vergelichen
   (also andere ist und soll quelldateien). die soll quelldatei könnte das tiger cut sein

- in write_rxncon_xls(inputdir, rxncon) sage ich ganz oft, dass eine information nicht given ist. Stimmt das wirklich? oder kann man die doch irgendwo finden?
- in write_rxncon_xls(inputdir, rxncon) bin ich auch nicht sicher ob ich die infos richtig einsprtiere (gibt auch comments dazu, geht um name und ID)
- in write rxncon_xls werden inn die reaction definitions noch reactionen geprintet, die in der beispiel datei nicht dabei sind mit der überschrift Genetic evidence only (g_) or mapped from review paper (r_):
- in write rxncon_xls allet schön bunt machen
- in write rxncon_xls klatsche ich einfach alle Gene in (VI) ORF IDs, ist das richtig? kp welche non ORF (VII) sind


- txt format sbtab testen und hello() anpassen
- csv format sbtab testen
- xls format sbtab testen
   - für diese drei tests mit bngl output arbeiten (siehe oben, marcus)
- txt format rxncon testen
- xls format rxncon testen
   



   Ferne Zukunft: rxncon xls File erstellen:
   - einträge in contingencie list als link auf komponenten in
      reaction list erzeugen
      "Instead of typing the states manually, it is recommended that
      you link your target and modier to the respective cells in the
      reaction list.  By doing so, you will keep your network denition
      internally consistent, which will save you a lot of time in case
      you later change the name or reaction of a component"